# Outlook / Exchange credentials for or.even@cubelogic.com
EMAIL_ADDRESS=or.even@cubelogic.com
EMAIL_PASSWORD=your_password_here

# Exchange server settings (leave SERVER blank to use autodiscover)
# EXCHANGE_SERVER=outlook.office365.com

# Recipient email for the summary (defaults to EMAIL_ADDRESS if not set)
RECIPIENT_EMAIL=or.even@cubelogic.com

# Optional: override the target month (YYYY-MM). Defaults to the previous calendar month.
# TARGET_MONTH=2026-05
